/// Visual variants supported by [DSButton] and [DSIconButton].
enum DSButtonVariant {
  /// High-emphasis button for primary actions.
  primary,

  /// Medium-emphasis button for secondary actions.
  secondary,

  /// Button with transparent background and visible border.
  outline,
}
