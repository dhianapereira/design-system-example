/// Icon positions supported by [DSButton].
enum DSButtonIconPosition {
  /// Renders the icon before the label.
  start,

  /// Renders the icon after the label.
  end,

  /// Renders the icon above the label.
  top,

  /// Renders the icon below the label.
  bottom,
}
