/// Dimensional variants supported by [DSTextField].
enum DSTextFieldSize {
  /// Compact field size for dense interfaces.
  sm,

  /// Default field size.
  md,

  /// Spacious field size for prominent forms.
  lg,
}
