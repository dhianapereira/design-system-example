/// Visual treatments supported by [DSBadge].
enum DSBadgeVariant {
  /// Neutral badge for low-emphasis metadata.
  neutral,

  /// Primary badge for high-emphasis metadata.
  primary,

  /// Secondary badge for low-emphasis branded metadata.
  secondary,

  /// Error badge for destructive or invalid states.
  error,
}
