/// Padding options supported by [DSCard].
enum DSCardPadding {
  /// Removes the inner spacing.
  none,

  /// Compact spacing for dense layouts.
  sm,

  /// Default spacing for most cards.
  md,

  /// Spacious spacing for prominent content.
  lg,
}
