/// Dimensional variants supported by [DSDropdown].
enum DSDropdownSize {
  /// Compact dropdown size for dense interfaces.
  sm,

  /// Default dropdown size.
  md,

  /// Spacious dropdown size for prominent forms.
  lg,
}
